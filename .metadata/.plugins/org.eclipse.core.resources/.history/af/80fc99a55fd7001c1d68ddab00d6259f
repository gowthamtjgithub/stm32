/*
 * 002led_button.c
 *
 *  Created on: 19-May-2022
 *      Author: gowth
 */

#include "stm32f4xx.h"

void delay(void){
	for(uint32_t i=0; i<50000; i++);
}

int main(void)
{
	GPIO_Handle_t GPIOBtn;
	GPIOBtn.pGPIO_PinConfig->GPIO_PinNumber = GPIO_PIN_6;
	GPIOBtn.pGPIO_PinConfig->GPIO_PinMode = GPIO_MODE_OUT;
	GPIOBtn.pGPIO_PinConfig->GPIO_PinSpeed = GPIO_SPEED_FAST;
	GPIOBtn.pGPIO_PinConfig->GPIO_PinOPType = GPIO_OP_TYPE_PP;
	GPIOBtn.pGPIO_PinConfig->GPIO_PinPupdControl = GPIO_NO_PUPD;

	GPIO_PeriClockControl(GPIOA,ENABLE);
	GPIO_Init(&GPIOBtn);

	while(1)
	{
		GPIO_ToggleOutputPin(GPIOA, GPIO_PIN_7);

		delay();
	}
	return 0;
}

